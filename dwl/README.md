# dwl Configuration

Custom dwl compositor configuration – keybindings, colors and optional patches.

## Files

- `config.def.h` – main configuration (keybindings, colors, rules, layouts).
- `config.mk` – build options 
- `patches/` – any applied patches

